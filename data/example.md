# 示例文档

这是一个示例文件，用于展示支持的文档格式。你可以删除此文件并上传自己的文档。

## 支持的格式

- **Markdown** (.md) - 自动解析为纯文本，去掉标记
- **纯文本** (.txt) - 自动检测编码（UTF-8/GBK 等）
- **Word** (.docx) - 提取段落和表格内容
- **PDF** (.pdf) - 提取文本和表格

## 使用方法

1. 将文档放入 `data/` 目录
2. 在网页端点击"构建索引"或重启服务
3. 开始问答
